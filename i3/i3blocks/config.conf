separator=true
separator_block_width=10

# Data e Hora
[date]
align=center
command=~/.local/bin/i3datetime
interval=60

# Ethernet / IP
# [ethernet]
# command=ip route get 1 | awk '{print "IP: " $7; exit}'
# interval=5
# label=

# CPU
# [cpu]
# command=grep 'cpu ' /proc/stat | awk '{usage=($2+$4)*100/($2+$4+$5); printf "CPU: %.0f%%", usage}'
# interval=5
# label=

# RAM
# [memory]
# command=free -m | awk '/Mem:/ { printf "RAM: %d%%", $3*100/$2 }'
# interval=5
# label=

# Volume
# [volume]
# command=wpctl get-volume @DEFAULT_AUDIO_SINK@ | awk '
# /MUTED/ { print "VOL: muted"; exit }
# { printf "VOL: %d%%", $2*100 }'
# interval=1
# label=

# Microfone
# [mic]
# command=~/.local/bin/i3mic
# interval=1
# label=
